# Roleplex 工程约定

## 文档职责与维护规则

项目文档按职责分层，避免同一事实在多处重复。修改前先判断信息属于哪一类；只更新权威文档，并在其他文档中使用链接引用。

### README.md：项目事实与使用指南

- 维护项目背景、产品范围、当前阶段、开发环境、安装启动方式、依赖、环境变量、端口和数据库使用说明。
- 每个里程碑完成或范围变化后，更新当前阶段，明确已实现、部分实现和未实现内容。
- README 面向开发者和使用者，回答“项目是什么、现在能做什么、如何运行”；不复制接口字段、内部事件细节或实现推理。

### docs/protocol.md 与协议文档体系：接口契约

- `docs/protocol.md` 是稳定入口，维护协议范围、通用总则、版本/兼容性规则、状态标签、领域索引和文档维护流程；它不是必须容纳全部接口的唯一大文件。
- 具体协议按领域和复杂度拆分到 `docs/protocol/`，优先按领域而不是按单个 endpoint 切碎。公开协议可按 REST、消息、WebSocket、资源和横切规则组织；内部协议和测试契约单独归类。
- 每篇领域协议应标注受众（公开/内部）、状态（已实现/原型/预留/废弃）、协议版本、维护者、事实来源和复核日期，并说明请求响应或事件、鉴权、资源归属、幂等、版本、并发、错误、降级行为、关联代码和测试。
- API、WS 事件、字段、状态值或错误码发生变化时，只更新对应领域权威文档，同时同步服务端 schema、客户端类型和测试；最后更新 `docs/protocol.md` 的索引或状态，不复制完整字段。
- 协议版本以领域为优先维度；兼容新增字段或事件不复制整套 v1/v2 文档，只有不兼容并行协议才新增版本入口或迁移文档。
- 公开协议只承诺客户端可依赖的 wire contract；内部 Agent、MCP、调度和审计约定不自动获得公开兼容承诺。没有对应服务端处理和客户端行为的内容，不得描述为当前可用功能。
- Agent 修改接口时，先从路由装配、schema、客户端类型、错误码和测试反查领域文档，不得假设所有契约都在 `docs/protocol.md`；完成后检查索引、README 和测试是否需要同步。
- 当入口文件超过可扫描范围或某领域出现独立生命周期时，必须拆出领域文档，禁止继续向 `docs/protocol.md` 堆叠完整 endpoint/event 清单。
- 不兼容变化必须更新协议版本或提供迁移说明；新增内容优先保持向后兼容，客户端对未知事件或消息类型提供降级行为。
- 示例只能使用占位数据，不得包含真实凭据、隐私或危险命令。

### CLAUDE.md：工程规则

- 只记录跨任务稳定的开发规则、安全边界、测试要求和文档维护方法，不复制 README 的项目事实或协议字段。
- 不把具体实现名称、字段名、库名或临时环境值写成长期规则，除非它们是明确的兼容性约束或已经定稿的工具链选型；具体细节应放在代码、README 或协议文档中。
- 产品范围、运行方式或接口契约改变时，检查 README、`docs/protocol.md` 和测试是否需要同步更新；不为了“同步”而复制内容。
- 认证、资源授权和工具权限修改必须保持项目既定的 Owner/Guest 安全边界，并以 README 和协议文档为上下文。
- 架构计划用于记录架构决策、里程碑范围和验收标准；计划与实现规则冲突时，先暂停编码并更新计划，经确认后再实施。
- 未经计划明确纳入并更新验收标准，不得把 P2 预留能力实现为当前功能。
- 文档优先级按职责划分：本文件的工程规则约束工作方式；README 的项目事实约束产品范围和运行方式；协议文档约束对外接口；架构计划约束已批准的设计和里程碑。出现冲突时，先修正文档再改代码。

## 数据库与迁移纪律

- 业务数据访问使用数据库无关的 ORM 和通用 SQL 类型；不得在业务层写 SQLite 或 PostgreSQL 方言 SQL。
- JSON 数据使用通用 JSON 类型；主键和排序键使用可迁移的标准整数或明确的跨数据库类型；不得依赖某一数据库的隐式行为。
- 搜索和过滤优先使用可迁移的查询；不得未经计划评审引入特定数据库专属全文检索或索引特性。
- 数据库连接参数必须来自配置，连接级优化必须隔离在数据库适配层，不得散落到业务逻辑。
- 新增或修改表、字段、索引、约束时，必须提供对应 Alembic 迁移，并验证迁移在 SQLite 和目标 PostgreSQL 方言上可重放；没有可用 PostgreSQL 实例时，至少执行目标方言的 Alembic 离线 SQL 编译检查，完整重放放入 CI 或数据库切换评审。
- SQLite 仅作为单进程、单 worker 的开发和轻量部署方案；需要多 worker 或常态化并发写入时，先按计划切换 PostgreSQL，不得用业务锁掩盖 SQLite 单 writer 限制。

## 并发与写入纪律

- 所有数据库写入使用短事务；遇到 SQLite 临时锁时使用有限次数的退避重试，并保证重试操作具备幂等或明确的失败语义。
- 一条消息只能由一个明确的 reducer/所有者任务修改；并发更新使用 revision 或等价乐观锁，禁止静默覆盖新数据。
- 流式增量优先保存在内存并广播，按节流间隔、part 完成或最终完成落库；不得逐 Token 建立数据库写事务。
- 会话队列只负责触发顺序和业务调度，不得被当作全库写锁；跨会话和 Orchestrator 并行任务仍必须遵守全库写入纪律。

## 工具安全与 Agent 架构边界

- 工具权限默认封闭：只有显式白名单工具可以标记为 safe；MCP 工具、未知工具和有副作用的内置工具默认 dangerous。
- 模型厂商 API Key 必须加密落库，接口只返回不可逆的脱敏标识；凭据不得写入源代码、测试固件、示例数据或持久化日志。
- 工具危险级别必须在服务端执行层判断和拦截，不能只依赖系统提示词、工具名称或模型自述；Owner/Guest 权限、人工确认、配额和审计规则必须在工具调用前生效。
- LangGraph 或其他 Agent 框架的事件必须先经过防腐层转换为项目领域事件；业务调度、数据库、WebSocket 和前端只消费领域事件，不得直接依赖框架私有事件名。
- Agent 并行任务必须有限制、有父子链路、有取消和失败降级语义；不得让子任务重新获得超出授权范围的调度工具。

## Windows 运行时与 MCP

- Windows 后端保持默认 Proactor 事件循环；不得设置 `WindowsSelectorEventLoopPolicy`，否则 stdio MCP 子进程可能无法运行。
- 启动 stdio 子进程前解析实际可执行路径；Windows 下 `.cmd` 包装器不得假设由底层进程自动解析。
- 子进程环境必须显式继承运行所需的系统路径和编码设置；不得把 API Key、Token 或完整 MCP 凭据写入启动日志。
- MCP 会话的打开和关闭必须在同一个宿主异步任务内完成；取消、超时和服务关闭时必须回收子进程及其进程树，不能只释放 Python 对象。

## 代码注释与函数说明

新增或修改代码时必须同步维护注释，标准如下：

- 项目代码注释和文档说明默认使用中文；Python 使用简洁中文 docstring，TypeScript/React 使用中文 JSDoc/TSDoc。只有代码语法、协议字段、库 API 名称、标准错误码或既有外部约定必须保留时，才保留对应英文原文，并可在首次出现时补充中文解释。
- 具名函数、导出函数、异步业务函数和包含业务规则的 React 组件，要有简短职责说明。
- 函数有参数时，逐项使用 `Args:`（Python）或 `@param`（TypeScript）说明参数用途、来源或边界；无参数函数不写虚假的参数说明。
- 返回值、异常或副作用不明显时，补充 `Returns:` / `Raises:` 或 `@returns` / `@throws`；显而易见的返回值不机械复述。
- 关键部分解释“为什么”和边界条件，尤其包括鉴权隔离、敏感数据处理、并发与一致性、幂等与版本、断线恢复、工具权限、资源授权和失败降级。
- 注释不得逐行翻译代码，不为简单 getter、显而易见的一行转换、JSX 视觉标记或内联回调堆砌无价值注释。
- 注释不得包含真实凭据、用户隐私或仅在本机成立的临时值。
- 修改实现时同步维护相关注释；涉及对外行为时，按文档职责规则更新 README 或 `docs/protocol.md`。

## 日志与链路追踪

日志必须支持从一次用户请求、一次消息处理或一次 Agent 调度，追查到完整的调用链和最终错误。新增异步任务、并行 Agent、工具调用和重试逻辑时，必须沿用并传递同一条链路上下文，不能只依赖进程、线程或时间戳猜测关联关系。

- 机器消费的日志输出必须使用结构化格式（推荐 JSON）；开发终端可以有简短可读格式，但不得取代结构化记录。
- 每条业务日志至少包含：时间、级别、模块、稳定事件名称、请求链路标识、会话标识、消息处理链标识和 Agent/工具执行标识；并行调度额外记录父执行标识、角色标识、任务阶段、开始/结束状态和耗时。缺少某个上下文时必须明确记录为空，不得伪造或用时间戳替代。
- 事件名称和字段必须稳定且可检索；禁止只输出无法关联的自由文本或仅打印“发生错误”。
- WebSocket 和流式消息日志还必须能关联进程 epoch、会话事件序号、消息 revision、增量序号及工具审计记录标识，确保能够排查丢失、重复、乱序和重启恢复问题。
- 链路上下文必须跨越 HTTP、WebSocket、数据库事务、asyncio Task、并行任务、Agent 调用和工具调用；优先使用 `contextvars` 或等价机制，并在创建后台任务时显式复制/绑定上下文。
- 关键生命周期至少记录：请求接收、鉴权结果、消息入队、任务开始、模型请求开始/结束、工具开始/结束、重试、取消、落库、事件广播和最终完成/失败。
- 异常日志必须包含错误类型、稳定错误码、完整堆栈和链路上下文；预期的取消、超时、重试和降级应按对应状态记录，通常不作为 ERROR，不能把正常取消伪装成未处理异常。
- 流式增量不得逐条写日志；按开始、阶段变化、节流摘要和最终结果记录，必要时附带数量、长度或耗时。
- 默认不得记录密码、API Key、Token、Authorization 头、完整用户输入、完整模型输出或 MCP 敏感参数；仅允许在本地显式开启临时 debug 时记录去除凭据后的完整请求结构或脱敏截断摘要，不得持久化到正式日志，不得读取并破坏流式响应体，调试结束后必须关闭。
- 日志字段和错误码发生变化时，检查监控/排障脚本和测试是否需要同步更新；日志上下文不得成为新的接口事实来源。

## 分层测试要求

测试必须按风险选择层级，浏览器 E2E 不能替代后端、并发、Agent 和协议测试。

- 后端单元/集成测试使用 pytest 和 httpx，覆盖 Owner/Guest 权限矩阵、资源归属、错误码、Token 撤销、数据库迁移和安全边界。
- 涉及原子兑换、Owner 初始化、配额或并发写入的逻辑，必须有并发测试；测试使用每轮全新的数据库，不能依赖共享单例状态或仅更换用户名。
- Agent 调度、工具循环、取消、重试、Orchestrator fan-out 和框架事件映射必须使用确定性的 mock/fake provider 测试；不得让普通回归测试依赖真实模型计费或不稳定输出。
- WebSocket 事件恢复必须进行断开、重连和事件序列比对，至少验证丢失、重复、乱序、进程重启和快照兜底场景。
- MCP 测试必须验证连接生命周期、超时、取消、工具授权和子进程/进程树清理；允许使用受控测试服务器，不得让测试访问真实用户文件。
- 对真实模型厂商的流式、工具、图片、usage 和错误格式验证使用独立的契约测试；真实 Key 测试默认可选，不作为普通 CI 的必需条件，并且必须脱敏日志。
- 产出用户可见行为的里程碑必须同步增加 Playwright 浏览器端到端测试；纯风险验证或无用户界面的 M0 spike 不强制浏览器流程。
- Playwright 测试必须启动真实前端和后端服务，并通过浏览器模拟用户操作；Agent 行为使用确定性 fake provider，真实厂商测试放在契约测试层。
- 每个用户可见里程碑至少保留一条可重复运行的 happy path 和一条关键失败/权限路径；测试失败时优先修复功能或测试环境，不得只依赖编译、API smoke test 或静态检查宣称完成。
- 使用 Playwright 截图或 trace 辅助检查真实页面表现；测试结束后清理临时数据和服务进程。测试数据必须隔离、可重复运行，尤其是包含全局初始化状态的测试。
- 测试选择器应依赖稳定的可访问名称或语义，不依赖易变的 CSS 细节。
