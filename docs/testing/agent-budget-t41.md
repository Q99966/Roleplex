# T4.1 计数与停止边界验证

- 状态：T4.1 离线核查完成；以下是该阶段历史基线，T4.2 修复与现行行为见[后续验证](agent-budget-t42.md)。
- 复核日期：2026-09-15；维护者：Roleplex。
- 权威口径与待实现边界：[Agent 执行预算](../protocol/internal/agent-budget.md)。
- 本轮环境：LangGraph 0.2.74、langchain-core 0.3.36、langchain-openai 0.3.1、openai 1.109.1。升级时重新验证，不能照搬换算。

## 结果

持续请求工具的确定性脚本：

| 图上限 | 每轮工具数 | 每工具逻辑节点 | 模型决策 | 实际工具开始/完成 | 已处理逻辑节点 | 未派发提议 |
|---|---|---|---|---|---|---|
| 2 | 1 | 1 | 1 | 0/0 | 0 | 1 |
| 4 | 1 | 1 | 2 | 1/1 | 1 | 1 |
| 15 | 1 | 1 | 8 | 7/7 | 7 | 1 |
| 15 | 3 | 1 | 8 | 21/21 | 21 | 3 |
| 15 | 1 | 8 | 8 | 7/7 | 56 | 1 |
| 16 | 1 | 1 | 8 | 7/7 | 7 | 1 |

逻辑节点使用无磁盘副作用的工具替身计数，不冒充真实文件提交数量。每次调用开始/结束身份配对，最后提议保留未派发记录，没有额外模型总结。fake 不伪造 usage。

HTTP 层使用真实 ChatOpenAI/SDK + httpx.MockTransport，完全离线：首次 500 后重试成功或再次失败，均有 2 次 HTTP 尝试但只有 1 次 ProviderCallStarted；成功有 1 次 Completed，耗尽时为 ProviderError。仅记录数量，不保存请求头、正文或真实凭据。

纯文本短任务在上限 2 正常完成。另用七轮工具后返回纯文本的脚本发现：上限 15 时文本已到达却标 graph_budget，上限 16 则 completed。已写入明确复现并纳入 T4.2；后续已将此测试更新为两种上限均正常完成，不改写本记录的历史结果。

## 复跑

在 `backend` 执行：

```bash
python -m pytest -q tests/test_agent_budget_accounting.py tests/test_agent_loop.py tests/test_execution_summary.py --tb=short --show-capture=no
```

先完成三组回归 **41 passed（10.72 秒）**，随后补齐最后纯文本的 15/16 边界，预算专项 **11 passed（0.98 秒）**。两者有重叠，不相加为独立测试总数。

本步只增加诊断性测试与契约/注释校正，不改变公开 API、数据库、日志字段、前端或模型参数；未运行浏览器与真实计费模型测试。SDK 重试和图边界通过离线受控时序验证，不以真实模型不稳定输出代替确定性证据。

## 下一小步

后续顺序以 [T4 小阶段与退出条件](../plan/tool-reliability-capability-roadmap-v1.md#85-小阶段与退出条件)为准：T4.2 先修复准确停止及最后工具结果交接，预算配置与快照归 T4.3，继续能力归 T4.4。本记录仅证明 T4.1 现状，不表示上述功能已实现。
