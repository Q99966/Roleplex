# 角色最大输出设置验证

> 本页保留对应阶段的验证记录与参考步骤，不表示本次已重新运行。当前测试选择、环境与收费执行条件见[测试指南](README.md)；历史排期和验收关口不自动约束新任务。

状态：已实现，已通过人工验收；复核日期：2026-09-15。

本次只调整角色表单：最大输出数字输入、1K–64K 快捷选项、厂商默认；按用户后续要求移除高级 JSON 入口，工具模块占满整行。
仍使用既有 params.max_tokens 接口；其他参数原样保留，不自动给空参数角色增加输出上限。
上下文的默认 1,024 预算预留明确标注为本地估算，不冒充厂商上限。
同时修正上下文输入 step=1024 导致默认 200000 被浏览器判为无效、阻止保存的问题，现允许整数窗口值。

## 验证

frontend 下执行：

```bash
npm run build
npm run test:e2e -- role-output-settings.spec.ts
```

构建通过，真实前后端浏览器用例 1 passed（7.0 秒），测试通过 REST 读取后端保存值，覆盖：

- 已有 max_tokens=1024 回显；快捷 8K 保存为 8192，自定义 12345 保存并刷新恢复。
- temperature 与 stop 参数不被覆盖；厂商默认只删除 max_tokens，刷新后保持未指定。
- 高级 JSON 入口不存在；超过有效窗口的数值阻止保存，后端原值保持。
- 1280/390 宽度工具模块与输出设置同宽且无横向溢出，工具开关仍可操作并保存。
- 类别折叠不改变选择，折叠时可全选；全选/取消只影响本类别，部分选中显示半选，保存后重新打开状态准确。

报告：logs/tests/e2e/fake/2026-09-15/09-22-48_5fe346f3/。
中间一轮遭遇宿主时钟不足一秒回拨，导致刷新后的新 JWT 被拒绝；未修改认证规则，重跑通过。
测试服务已清理；未调用付费 Provider，本次验证表单与后端参数保存，不改模型适配器或推理配置。
