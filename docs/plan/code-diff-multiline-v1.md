# 代码 diff 与多行输入体验计划 v1

| 元数据 | 值 |
|---|---|
| 受众 | Roleplex 前后端、工具安全、协议与测试维护者 |
| 状态 | v1.1 已获用户批准；M 已通过人工验收并获准提交，G 服务入口契约仍需定稿，D/E 未实施 |
| 计划版本 | 1.1（工具职责评审修订） |
| 维护者 | Roleplex |
| 复核日期 | 2026-09-11 |
| 上游计划 | [工作区与代码工具](agent-repository-workspaces-v1.md)、[工具时间线与详情](tool-timeline-details-v1.md)、[会话后台服务](conversation-services-v1.md) |
| 事实来源 | `frontend/src/components/ActiveWorkspace.tsx`、`ToolCallCard.tsx`、`store/chat.ts`；`backend/app/workspaces/files.py`、`tools.py`、`agent/tool_capture.py`、`agent/loop.py`、`services/tool_details.py`、`routers/messages.py` |

## 一、目标、顺序与范围

本计划解决用户提出的两项体验问题：输入框无法换行，以及无法在工具实际执行位置查看代码增删。
用户已批准本计划的方向、阶段安排与停服编辑策略，并随后授权实施 M；不自动提交，不提前实施 G 服务契约变更或 D/E。

已批准在 W1d 之后、W2a 之前插入以下阶段：

1. **M：多行输入。** 先完成较小的输入体验闭环，自动验证后交用户人工验收、独立提交。
2. **G：工具职责与编辑路径评审。** 可先完成设计，但不阻塞 M；G 的三个决策得到用户确认后才能实施 D/E。
3. **D：原生编辑的 diff 采集与展示。** M 人工验收、G 定稿后实施，保留原 write 的采集设计，独立验收与提交。
4. **E：最小局部编辑工具。** 建议紧接 D，复用已验证的采集通道，独立验收与提交；不等到 W3 才评审。

D 通过只能称为 write 单项记录可用；D/E 的整体体验验收还需通过日常完整工具集测试，不能把入口受限的测试
当作日常代码修改已完整可视化。G 如决定收窄服务参数，须先安排独立服务契约迁移切片，再验收工具组合。

上述插入顺序已获确认；不改写 W2a/W2b/W3 的既定范围。
单条超长消息分块 C 继续延期，不因 diff 的体积限制引入消息分块协议。

## 二、现状与能力边界

- 输入框是单行 `input`。`/ps` 有独立键盘入口，普通 Enter 依赖表单提交；没有统一的换行、中文输入法和补全优先级。
- 前端发送正文用 `trim()`，后端也用 `strip()` 校验首个文本段；实施前需沿落库与模型上下文链路核对，不能只换成 `textarea`。
- 原生工具只有 `workspace_list/read/write`。`workspace_write` 可创建或携带 expected hash 替换 UTF-8 文件，
  返回新建标记、字节数与 hash；没有独立局部编辑、补丁、删除、重命名工具，也没有修改前后的 diff 记录。
- 工具时间线已有稳定调用位置，Owner 详情有独立鉴权、World 加密与 7 天保留期，可复用而不再造共享正文通道。
- Shell 和后台服务脚本可以修改文件。当前服务存续期间，原生 `workspace_write` 仍被禁止；Shell 可逐次申请审批。
  该不对称策略纳入 G 重新评审；评审定稿前保留现行行为，不把原生工具记录解释为全工作区写入审计。

## 二补、G：工具职责与编辑路径关口

### G.1 评审依据与必须确定的事项

单有可信 diff 不保证日常可见：如果更方便的修改入口是服务脚本，或服务运行使正规编辑入口不可用，
只覆盖 write 的页面会长期缺少记录。应降低正常编辑的成本，并如实表达其他入口的能力，而非要求模型把每个工具用一遍。

现有 W1d 明确要求逐次审批完整脚本、cwd、端口、探针与寿命，并冻结审批计划；不能未经检查就说界面隐藏了脚本。
服务存续期间拒绝原生写入是保留冲突边界、暂不承诺 HMR 的保守策略；允许已审批 Shell 后，它不再形成一致的文件写入隔离。
G 必须给出以下三个决策，并同步相应权威计划，不能只改工具描述或删除一个运行态检查：

| 决策 | 建议方向 | 实施前必须明确 |
|---|---|---|
| 局部编辑入口 | 将最小 `workspace_edit` 提前为 E | 精确匹配、hash、授权、运行占用策略、失败语义与 diff 复用 |
| 服务运行中编辑 | 已批准：停止 → 编辑 → 重新审批启动 | 涉及其他会话服务由 Owner 协调，不得擅自代停；清理未确认不得编辑 |
| 服务启动入口 | 优先启动已有项目入口，评审 Owner 确认的结构化启动配置 | 是否撤下任意脚本模式、允许的启动器/参数、配置与项目入口变更复核、旧审批兼容及独立实施范围 |

第三项尚非接口定稿。若继续保留任意启动脚本，审批和工具说明须明确“脚本可准备项目、修改文件及执行其他操作”，
并明确 diff 覆盖缺口；不能同时承诺它只能启动已有项目。若选择收窄，应由后端接受明确配置/schema 并拒绝契约外字段或模式，
不能以禁止换行、`&&`、命令名黑名单来声称识别了任意编辑脚本。解释器内联执行参数也不能悄悄成为同等任意脚本入口。

结构化启动不等于只读运行：项目入口、包管理器生命周期脚本、依赖及子进程仍可能写文件；批准前后的入口文件变化
也需要明确复核语义。首版不实现跨平台进程沙箱，不承诺无外部副作用或完整写入归因。
收窄现有服务参数属于兼容性变化，必须明确旧客户端、待审批请求及已运行实例的处理，不能直接套用新校验而误停服务。

### G.2 最小局部编辑候选

候选 `workspace_edit(path, old_text, new_text, expected_sha256)`，字段名与最终错误码在内部工具协议定稿：

- 只修改已有 UTF-8 普通文件；old_text 非空，按字面精确匹配，包含重叠候选在内必须恰有一处，否则拒绝且不写文件。
- 不做正则、模糊匹配、自动缩进修复或自动全局替换；new_text 可为空以删除片段，但不删除文件。
- expected hash 必填，校验完整文件版本；同一份已读取、校验内容完成匹配和新内容构造，不通过额外无保护的读写拼接。
- 沿用 write 的路径、敏感内容、大小、授权、运行占用及提交前复核；所有实际写入复用共同受控写路径，不能复制出第二套安全实现。
- 输入片段有字节上限，结果文件仍受 1 MiB 限制；G 定稿预算时覆盖大文件小修改场景。
- 复用执行处前后证据和 Owner diff。不可仅把 old_text/new_text 当成已成功修改；失败、不变及结果未知均沿用 D 的语义。
- E 是 managed_directory 的单文件字面替换，不提前开放 W3 的 Git worktree、多文件补丁或删除能力。

### G.3 服务占用下的正规工作流

本轮已批准采用停服编辑：工具拒绝须说明需要停止阻塞编辑的托管服务，Owner 通过现有授权流程协调；
确认相关占用释放后 edit/write，再以新审批启动。停止一个服务不代表同工作区其他服务均已结束，
不能自动停止其他会话服务，也不保证外部编辑器和非托管进程停止写入。

若用户选择运行中原生编辑，必须单独设计服务启动/停止与编辑的竞争、文件版本、服务就绪状态和清理门槛，
更新 W1d 计划并明确验收；不能只删除占用判断。本草案不预先批准 HMR 或运行中编辑。

用户提出未来可由 Agent 临时拉起群聊，协商哪些服务需要停止以便修改；本轮只记录为后续方向，不实现自动建群、
跨会话协商或新的停止权限。当前受其他会话服务阻塞时，应报告原因并等待 Owner 协调，不自动批量停服。
未来即使协商达成一致，仍须由服务端按服务归属、授权和实时状态执行；聊天中的同意不自动产生跨会话控制权，
也不代替重新启动的 Owner 审批。该能力待相关 Agent/群聊架构阶段另行设计与验收。

### G.4 两类变更记录不混用

- **工具编辑记录：** 本次 write/edit 确认提交的前后变化，放在原工具卡；D/E 交付。
- **工作区观察记录：** 某个明确基线与检查点之间观察到的净变化，可能混合 Shell、服务、人工和其他会话写入；后续设计。

后者必须标明时间/基线、范围、忽略项、不完整情况及来源未精确归属；不能从服务启动调用返回推断后续文件变化结束，
也不能把区间净变化命名为某工具完整审计。本轮不增加扫描器、快照或观察记录接口。

## 三、M：多行输入

### 3.1 输入与键盘规则

- 使用原生 `textarea`，初始一行，按内容自动增高；建议最大高度为 `min(240px, 30dvh)`，超出后输入区内部滚动。
  发送/停止按钮保持可见，长粘贴不把历史区域挤出视口。
- 桌面端 Enter 发送，Shift+Enter 换行；按人工反馈不常驻显示快捷键说明。移动端软键盘保留换行，发送使用明确按钮。
- 输入法组合期间及确认候选的 Enter 不发送；兼容浏览器组合事件顺序，不靠固定延时猜测组合结束。
- 键盘处理顺序：输入法保护 → Shift+Enter 换行 → 有活动选择的 @ 补全确认 → 普通 Enter 提交。
  补全开启时方向键选择、Escape 关闭，首次 Enter 只确认补全；下一次才按提交规则处理。
- 多行中间编辑以光标位置识别 @ 查询，只替换光标附近的片段并保留后续正文；不继续只匹配整段草稿末尾。
  mentions 仍是显式角色身份，不从任意粘贴正文自动生成角色授权或调度目标。
- `/ps` 仅在整份草稿去除外围空白后恰好等于该命令、且用户明确提交时打开本地面板。
  Shift+Enter 不执行；含其他正文、代码围栏或多条指令的消息不当作管理命令。Owner/Guest 既有边界不变。
- 按钮、Enter 和本地命令走统一入口，避免重复提交；保持既有群聊队列、停止与加载/同步门槛，不顺带改变生成期间的调度策略。

### 3.2 正文、草稿与布局

- 仅用去空白结果判断是否为空；实际发送、存储及再次读取保留正文首尾空格、缩进和换行。
  以浏览器 textarea 的 LF 换行值为准，不承诺粘贴前 CRLF 字节级还原；不改变 text part 结构或模型历史窗口。
- 粘贴多行文本只插入内容，不发送；空白消息不发请求。
- 加载、同步未完成或提交失败时保留草稿与 mentions；成功确认后仅清理属于该提交且未被用户继续修改的草稿。
  异步结果不得清除新输入或其他会话草稿，不自动重发结果不明的请求。
- 本阶段不增加跨会话、刷新后或跨设备的草稿持久化；会话切换仍按既有页面生命周期处理，不能把旧草稿迁移到新会话。
- 输入框增高、缩回与工具详情展开都应保持既有历史阅读锚点；底部跟随只在用户处于底部时发生。

### 3.3 M 验收

- Playwright 真实前后端、fake Provider 验证多行粘贴、Enter/Shift+Enter、输入法组合、按钮发送以及仅发送一次。
- 验证多行代码缩进、前后空白、中文、空行在请求、落库、刷新和模型上下文投影中不被意外裁剪；空白消息仍被拒绝。
- 覆盖多行中间 @ 补全、群聊无 @ 不生成、角色失效、Owner/Guest 的 `/ps`、代码中的 `/ps` 不执行。
- 覆盖加载/同步阻止发送、HTTP 失败保留草稿、提交中继续编辑、切换会话后的迟到结果不污染当前输入。
- 窄屏、自动增高到上限、内部滚动与历史阅读位置截图检查；至少一条成功路径和一条失败/权限路径可重复运行。
- 输入法组合事件自动测试外，人工验收需使用真实中文输入法；模拟事件通过不冒充已验证所有输入法。
- M 不新增模型能力，不要求单独真实 Provider 计费测试。

### 3.4 M 实现与自动验证记录（2026-09-11）

- 输入逻辑独立为 ChatComposer，沿用碳黑/琥珀样式；textarea 按内容及宽度增高，超限内部滚动。
  Enter/Shift+Enter、组合状态/229、补全方向键与确认、独立 `/ps` 走明确的键盘优先级。
  补全按当前光标替换片段并保留后文，列表只滚动自身；窄屏收起成员侧栏，避免挤压输入区。
- 发送保持原文，服务端确认成功且草稿 revision 未变才清理；失败、加载/同步等待时保留草稿。
  会话切换后的旧请求不更新当前输入；草稿不持久化。Guest 依据公开会话成员 ID 开放输入，
  不再错误依赖不可见的 Owner 角色配置；后端仍负责实际成员、角色和工具权限校验。
- 后端原有 API 已按原文落库，本次修正模型投影裁剪并将内部 context schema 升至 2，
  同步内部协议与测试；不改公开消息结构、数据库、历史选择边界或服务工具契约。
- TDD 先确认旧输入框 Shift+Enter 错误提交、上下文投影裁剪缩进的失败，再完成实现。
  后端完整回归：210 passed、3 skipped、8 deselected；普通真实前后端 fake E2E：43 passed，
  含 9 条 M 专项；前端 build 与 diff 空白检查通过。无 schema 变化，未额外运行数据库迁移检查或真实 Provider 计费测试。
- M 专项涵盖真实剪贴板多行粘贴、原文请求与刷新、失败保留 mentions、提交中继续编辑、旧会话迟到成功/失败、
  组合事件、光标中间补全、Owner/Guest `/ps`、空白/代码不误触发、加载草稿、触屏键盘模拟及历史阅读锚点。
  桌面与 390px 窄屏截图已检查；输入法为事件模拟，不冒充真实输入法或手机实机验证。
- 最终 E2E 报告：`logs/tests/e2e/fake/2026-09-11/14-17-17_2259bcd6/summary.json`；
  测试库：`data/roleplex-e2e-20260911141717.db`。测试服务已退出，普通 E2E 端口已释放。

人工验收：刷新前端、重启后端后，检查中文选词 Enter 不误发、Shift+Enter 换行、多行代码粘贴及发送后刷新保留格式；
群聊中间插入 @ 后确认不丢后文，输入框增高时不打断历史阅读。手机软键盘请实机检查，发送使用按钮。
M 已通过下述人工反馈修订后的验收并获准独立提交；G 服务入口未定稿，D/E 及延期 C 均未实施。

人工反馈修订：移除底部常驻快捷键说明，调整单行输入高度与内边距，使占位文字/正文和发送按钮垂直居中，
多行自动增高保持不变。新增几何对齐回归先复现 6px 偏移，修订后 M 专项 10 passed、build 通过；
输入区截图已检查，测试服务已退出。用户随后确认该版本可提交，M 连同此显示修订通过人工验收。
此次提交只包含已批准计划、M 实现及测试/文档，不包含 G 服务契约改造或 D/E 实现；不将本次认可扩大为所有手机和输入法的实机覆盖。

## 四、D：现有写工具的可信 diff

### 4.1 首版覆盖与工具细化边界

- 保持 `workspace_write` 的模型调用参数及既有结果语义，增加执行层内部的修改证据采集，不要求模型自行生成 diff。
- 覆盖 UTF-8 文件创建、替换和写入相同内容；删除几行属于替换 diff，不等于新增文件删除工具。
  新建空文件即使 `+0/-0` 也必须标为“新建”，相同内容应明确“内容无变化”。
- 每次调用显示“本次修改”，不是相对 Git HEAD 的净变化，也不是整段回答或会话的汇总 diff。
  同一文件多次修改保留各自调用记录，不自动合并、抵消或挪动工具位置。
- D 不新增局部替换；最小局部编辑提前在 G 评审、E 实施并复用 D。仍不新增 `workspace_apply_patch`、
  文件删除/重命名、仓库扫描、文件监听、Git 或 worktree；与 W3 的路径安全和补丁工具设计保持衔接。

### 4.2 采集位置、并发与失败语义

- 在已经授权、解析路径并持有现有写入锁的文件执行层取得本次校验的旧内容及待写新内容；成功写入后才标记修改已应用。
  新建文件旧状态为“不存在”，不能与“原来是空文件”混同。记录前后内容身份，禁止展示时重读当前文件来拼历史 diff。
- 保持现有 expected hash 复核、路径/symlink/敏感文件规则和服务运行限制。授权失败、hash 冲突或写前拒绝不得展示“修改成功”。
- 现有进程内锁不能隔离外部编辑器、Shell 或服务。diff 表达本工具确认提交的内容变化，不保证页面展开时文件仍是该版本，
  也不承诺对外部写入的系统级 compare-and-swap；不能宣称完整捕获所有并发修改。
- 文件写入与数据库不是一个原子事务。写入成功但详情持久化失败时不得回滚文件、自动重试写入或伪造“未修改”。
  取消、进程崩溃与中间失败只记录已观察事实；无法确认提交结果时明确“结果未确认”，不显示可信成功 diff。
- 原生新建过程中可能出现部分写入异常；这类情况不得标为“未发生修改”。首版不借 diff 采集扩大为完整文件事务系统。
- 旧内容、新内容只在本次执行的受控内存中用于计算；不为展示持久化两份完整快照。
  私有采集结果沿执行作用域、防腐层和领域事件交给消息所有者，不能通过模型工具返回正文或公共框架事件旁路传播。
  不允许无界全局字典按路径保存原文；关联到实际调用身份，失败/取消/结束都释放，不能串 execution 或会话。
- 工具结果和 diff 采集结果独立表达。计算超预算或失败不把已成功写入改判为工具失败，也不触发模型重复修改。

### 4.3 预算与表示（建议值，待本计划确认）

- 沿用单文件最多 1 MiB 的写入边界；diff 运算另设预算，不假设 1 MiB 文本的比较一定便宜。
- 单次 diff 的前后输入合计最多 256 KiB、合计最多 10000 行；超过任一值不计算，返回“差异超出采集预算”。
- 序列化私有 diff 对象整体最多 64 KiB（含路径、hunk、行号、标志与转义），最多保留 1000 个展示行，默认上下文各 3 行。
  超长单行不得绕过字节预算；有界前缀展示必须带明确的不完整标记，不截坏 UTF-8 或 JSON。
- 算法必须有可执行的工作量上限；不能只用异步超时包装不可中断的同步计算。高重复行、巨长行等压力测试
  若不能证明有界，须在实施 D 的算法选择关口调整方案，不得先上线无限计算或长期占用事件循环。
- 增删统计只有完整计算后才称“总计”；仅保留部分内容时区分总计与当前展示量，无法完整计算则统计为空而不是 0。
- 结构化行携带增删/上下文类型及前后行号；保留无末尾换行、换行风格变化的提示。控制字符用可见转义呈现，不能静默清除后冒充原始代码。
- 本次 64 KiB 是独立私有详情预算，不改变已有历史分页预算，也不追加“加载完整超大 diff”的隐藏无限接口。
- 256 KiB 输入合计是候选而非已批准体验标准：约 130 KiB 的文件即使只改三行，前后合计也会超限。
  G/D 需用 120 KiB、130 KiB、接近 1 MiB 的代码及高重复行样本评估完整、降级和耗时；E 必须复测大文件小改。
  如日常降级不可接受，先评审提高预算或有界精确局部 hunk 采集，再定稿；不得为通过测试静默放宽运算上限。
  局部 hunk 若采用，须仍能证明是本次匹配、版本校验和成功提交的变化，不接受模型自报 diff。

### 4.4 私有存储、权限与协议

- 优先在既有工具详情密文中兼容增加带格式版本的写入差异表示，与通用 input/output 和 Shell 专用详情兼容。
  保留现有输入/输出能力，但 diff 自身预算独立，不能简单塞进截断的字符串再要求客户端解析。
- 具体字段、可用性、错误及兼容语义在 D 编码前写入工具详情领域协议，并同步内部采集契约和客户端类型。
  本草案不提前把候选字段变为已实现 wire contract；若需要表结构变更，必须先更新数据模型并提供迁移。
- 密文绑定实际 World、消息和调用归属，鉴权后解密；只允许当前 World Owner 及既有会话成员关系读取。
  Guest 不获得路径、代码或私有统计；共享工具 part 仅沿用安全状态/详情能力标记，不加入文件清单或 diff 正文。
- Owner 展开时按需请求；`no-store`，不进入 localStorage、共享历史缓存、机器日志、审计正文、浏览器 trace 或失败报告。
  收起、切换会话/World、退出登录时清除私有正文，并拒绝迟到响应。
- 沿用调用开始后 7 天展示期、启动清理过期密文与消息物理删除级联；回收站不可读。
  工作区解绑或外部文件变更不能把历史差异替换成当前内容；World 备份包含的密文仍按敏感业务数据保管。
- 历史调用未采集时明确“未记录”，不从现有输入、模型回答、当前文件或日志补造旧版本。
  区分等待执行、已记录、内容无变化、预算不足/部分展示、未记录、已过期、不可用与结果未确认。

### 4.5 前端交互

- 工具卡仍出现在本次调用的实际位置。“实时”指工具完成并保存结果后，已展开的 Owner 卡片自动更新，
  不等整条回答结束；不展示模型尚未执行的内容为已完成修改，不增加逐 token diff 广播。
- Owner 主动展开工具卡后显示文件相对路径、操作和可用的 `+/-` 统计；代码差异块可继续独立折叠。
  保留原始输入/输出入口，避免默认同时展开全文输入和 diff 造成重复堆叠。
- 首版使用统一行内 diff：删除行、增加行、上下文及行号；颜色同时辅以 `+/-`、标签和可访问名称。
  纯文本渲染，不执行 HTML、脚本、终端转义，不自动打开本机路径。首版不提供应用、撤销或回滚按钮。
- 默认限制展示高度并内部滚动；折叠和展开保持历史阅读锚点，不抢焦点、不强制滚到会话底部。
- Shell、后台服务和未知工具明确提示“此工具未采集文件差异，不代表没有文件修改”，不能展示空 diff 误导用户。
  不因模型说使用了编辑工具就视为有可信记录；不强制改写 Shell/服务权限或拦截脚本语法。

### 4.6 D 验收与实施关口

1. **契约与采集可行性：** 确认内部私有结果通道、调用身份映射、序列化预算、算法工作量边界和失败语义；
   如果需要修改模型可见工具参数或扩大存储/安全边界，暂停并回到计划确认，不夹带实现。
2. **后端确定性验证：** 创建/空文件/替换/不变、Unicode/换行、hash 冲突、敏感路径/symlink、服务运行拒绝、
   同文件连续与并发调用、异常/取消/崩溃窗口、文件成功但详情失败、计算和展示超限。
3. **权限与恢复：** Owner/Guest、非成员、跨消息/调用/World、密文替换、解密失败、7 天过期、回收站/物理删除；
   重连、重复/乱序事件与刷新后位置及差异一致。验证模型结果、共享 WS、机器日志和失败产物不含新增私有原文。
4. **浏览器：** 真实前后端加 fake Provider，按“前置说明 → 创建 → 中间说明 → 修改 → 最终回答”验证
   执行中/终态更新、二级折叠、行号增删、超限提示、刷新恢复、旧记录降级、阅读锚点与 Guest 拒绝；检查受控占位代码截图。
5. **World 与真实 Provider：** fake managed-world 验证切换隔离；独立 real-world smoke 在本轮隔离工作区内
   通过真实模型执行原生创建、读取、修改并核对 diff。只启用所需文件能力，避免 Shell/服务绕过测试目标。
   授权实施 D 即包含执行这一验收项；执行前说明联网计费，凭据仅进入后端加密边界，关闭 trace/video，不保存原始模型输出到报告。
6. **人工验收：** 用户检查实际位置、创建与修改效果、折叠、刷新、长差异和未覆盖工具提示；明确认可后再提交。

### 4.7 E 与完整工具组合验收

- 保留 D 的文件能力隔离测试，它证明单项正确性，但另外增加独立的完整工具集 fake/real-world 场景：
  启用日常文件、结构化命令、Shell 和服务能力，完成“创建/修改页面 → 启动预览 → 再修改页面 → 检查结果”。
  E 实施后包含 edit；按 G 定稿的服务策略执行，不能为了得到漂亮 diff 临时禁用其他入口。
- fake Provider 确定性覆盖 edit 唯一/无/多处匹配、hash 冲突、片段删除、相同内容、敏感路径、并发及详情失败不重复写入。
- 若服务契约收窄，构造确实超出定稿 schema/配置白名单的调用验证后端拒绝，不依赖模型主动生成违规请求；
  若契约仍允许任意脚本，则验证完整审批、能力说明及覆盖缺口，不把合法脚本写文件误测为必须拒绝。
- 真实模型选择非原生编辑路径不自动判失败；逐项记录哪些调用有可信记录、哪些不在覆盖范围、是否遵守审批与占用策略。
  不将“所有工具都调用过”作为指标。若正常工作流无法走通，或长期只有未覆盖入口，D/E 整体体验不得以单项通过代替验收，需回到 G。
- 检查服务启动返回后仍可能写文件的提示；Shell、服务及以后扩展的结构化命令不因名称或结构化参数被视为无副作用。
  当前固定 pwd/list/read/count 的事实不扩展成未来所有结构化命令的安全承诺。
- 服务运行中原生编辑按 G 定稿策略明确拒绝并提供可继续流程，或经独立批准后验证允许策略；覆盖其他会话占用、
  清理待确认、停服失败、重新启动需新审批，不能依赖杀掉全部测试进程规避竞争。
- 完整工具集 real-world 与单项 smoke 均为独立计费验收，执行前告知；使用受控文件与测试服务，
  不请求访问真实用户项目，不额外持久化私有内容。E 与工具组合人工验收完成后才宣称编辑体验阶段完成。

测试工作区按类别/时间戳/用例分层，使用独立数据库，遵守既有保留与清理规则，结束后回收启动的服务。
如涉及 schema，必须完成 SQLite 迁移重放及 PostgreSQL 方言离线编译；离线编译不能描述成 PostgreSQL 实例验证。

## 五、后续而非本轮交付

- 完整结构化补丁、批量/模糊替换与文件删除/重命名工具继续后置；最小精确局部编辑已移至 G/E。
- Shell/服务引起的任意文件变更：需要基线、快照或监听及并发归属设计，不承诺简单前后扫描能准确归因。
- 相对 Git HEAD 的仓库 diff、跨调用净变化、跨文件汇总、撤销/回滚、双栏比较与超大 diff 分页。
- 为运行中服务放开原生写工具、HMR 协调或强制所有脚本只通过编辑工具写入。
- 单条消息分块 C、消息虚拟化、持久草稿、富文本编辑器与 IDE 集成。

## 六、文档与批准点

本文件是本次范围、顺序和验收的权威计划；上游工作区计划只引用，不复制细节。
实施 M 时复核消息领域的正文保留契约；实施 D 时更新工具详情、内部文件执行与必要的数据模型/事件契约，
最后同步协议索引。README 只在实现及验收后更新可用能力，不将本草案写为已完成功能。

已确认：M → G 定稿 → D → E 的独立验收顺序（G 设计可先行）、最小局部编辑提前、停服编辑且不擅自停止其他会话服务、
Owner-only、7 天保留，以及真实世界单项与完整工具组合验收分别纳入 D/E。
仍需在 G 定稿：服务启动入口是否收窄及其兼容方案、编辑片段预算，以及规模测试复核后的 diff 运算预算。
批准计划不等于选择了尚未定稿的服务参数方案；在该关口关闭前不实施服务契约变更。
确认计划后也不自动开始 W2a、W3 或延期 C；后续实施、人工验收和提交授权分别记录。

### 2026-09-11 外部评审采纳记录

采纳用户提供的评审中关于工具组合覆盖缺口、局部编辑成本、服务占用矛盾、两类变更记录与完整工具集验收的意见。
保留原采集、失败、隐私与按需展示设计；不直接采纳“改成结构化参数便能禁止文件写入”的推论。
修订依据为用户提供的评审内容及本地 W1d 计划/实现，不将外部文章作为当前项目已具备隔离能力的证据。
本次只修订草案，不变更现行已批准服务契约；G 定稿后，涉及旧契约的变更先同步权威文档再编码。

### 用户批准记录

用户在 v1.1 评审后明确同意草案，并确认本轮采用“停服 → 编辑 → 重新审批启动”，不得擅自停止其他会话服务。
跨 Agent 临时群聊协商作为以后优化方向，不纳入本次实现。批准时功能尚未实施，用户随后明确授权实施 M。
