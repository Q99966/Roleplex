# Roleplex 工程约定

本文件只维护跨任务的工程规则。项目现状与启动方式见 [README](README.md)，文档职责和历史资料入口见 [文档导航](docs/README.md)。

## 任务范围与文档维护

- 以用户当前要求和本次任务明确采用的范围推进工作。已授权任务中的常规实现、修复、验证和文档同步可直接完成；不要因旧阶段编号、历史“待确认”、独立提交要求或人工验收记录反复请求批准。
- `docs/plan/` 区分已完成记录与待选方向，`docs/archive/` 只保留历史资料。历史计划中的排期、前置阶段、候选数值和“本轮不做”不自动约束新任务，也不自动授权扩展功能。只有本次任务明确采用的计划范围和验收项才适用。
- 发现文档与实现不一致时，核对相关代码和测试，修正过时说明；不能把代码中的安全或兼容性缺陷直接改写成合法契约。范围、破坏性变更或安全边界确实无法确定时，再提出具体问题；其余工作继续推进。
- 文档与实现可在同一变更中同步完成，无须为常规改动先走单独的计划审批。较大架构调整应记录目标、取舍与验收方式，避免把临时实现细节升级为长期规则。
- 按职责更新权威位置：README 写产品事实与运行方法；`docs/protocol/` 写接口、数据模型和内部契约；`docs/design/` 写仍适用的设计；`docs/testing/README.md` 写测试入口与数据管理。其他文档链接引用，避免重复字段、限制和命令。
- 修改 API、消息、WS 事件、错误码或数据模型时，反查路由、schema、客户端和测试，更新受影响的领域协议；只有范围或状态改变时才同步入口索引。兼容新增优先，不兼容变化说明版本或迁移方式，客户端对未知事件和内容保留降级行为。
- 示例使用占位数据，不包含真实凭据或用户隐私。完成报告区分实际验证、历史记录与尚未覆盖的内容。

## 数据库与一致性

- 业务查询优先使用 SQLAlchemy ORM 和通用类型；数据库方言、连接优化集中在适配或迁移层。引入数据库专属能力时说明可移植性影响，不把 PostgreSQL 离线编译当作运行验证。
- 表、字段、索引或约束变化必须提供 Alembic 迁移，并更新[数据模型](docs/protocol/internal/data-model.md)。运行 `backend/scripts/check_migrations.py` 检查 SQLite 重放、模型一致性和 PostgreSQL 离线 SQL；涉及实际 PostgreSQL 部署或专属行为时再补实例验证。
- schema 由迁移创建，不通过运行时建表或手工改表绕过迁移。仅可重建确认无保留需求的隔离开发/测试库，先停止其连接；真实数据走备份与正常升级。
- 写事务保持短小，SQLite 临时锁使用有限退避并明确幂等或失败语义。会话队列不是全库写锁；SQLite 单 worker 以及进程内调度限制见 README，多 worker 需连同调度、事件和运行时归属一起设计。
- 一条消息由明确的所有者/reducer 修改；并发更新使用 revision 或等价冲突检测。流式片段先在内存聚合，再按节流或完成边界落库，不逐 Token 开写事务。
- 文件和数据库没有跨系统原子事务。已提交、未执行、失败与结果未知必须分开，取消或崩溃不能抹掉已有事实，也不能盲目重放有副作用的操作。

## 权限、凭据与工具

- 保持 Owner/Guest、World、会话成员和资源归属隔离。新增入口、异步任务和子任务不能绕过这些检查；创建时的授权不能替代实际执行时的必要复核。
- 工具权限默认封闭：只有显式白名单工具可标为 safe；MCP、未知工具和有副作用的内置工具默认 dangerous。危险级别、工具开关、审批与已配置配额在服务端执行层落实，不依赖 Prompt、工具名称或模型自述。
- 模型 Key 加密落库，API 仅返回不可逆脱敏标识。凭据不进入源码、固件、示例、日志或浏览器测试产物；World 备份包含解密密钥，按敏感数据保管。
- 原生文件工具遵守授权根、链接、敏感路径与版本检查；Shell/后台服务的逐次审批和宿主访问边界以领域协议为准，不把工作目录、worktree 或进程管理当成系统沙箱。
- Agent 框架事件先转换为项目领域事件，调度、数据库、WebSocket 和前端不依赖框架私有事件。新增并行调度时定义父子关联、并发控制、取消和失败语义，子任务不扩大授权。

## 子进程与平台

- 修改 MCP、Shell、命令或后台服务时，覆盖连接/进程生命周期、超时、取消与进程树回收。只清除对象或发送停止信号，不等于已确认回收。
- Windows 后端保留支持异步子进程的默认 Proactor 事件循环，不设置 `WindowsSelectorEventLoopPolicy`；执行前解析实际可执行文件，正确处理 `.cmd` 包装器。
- 子进程环境显式传递所需路径和编码，不泄露凭据。MCP 会话在同一宿主异步任务内打开和关闭。
- 平台特定改动按受影响平台验证；没有实机证据时说明覆盖缺口，不用另一平台测试结果宣称通过。

## 注释与可维护性

- 项目说明和业务注释默认使用中文，协议字段、库 API 和标准名称保留原文。修改逻辑时同步修正附近过时注释。
- 公共接口、异步业务函数及包含业务规则的组件说明职责和关键边界。参数来源、单位、权限、返回值或副作用不明显时，用 Python `Args:` / `Returns:` 或 TypeScript `@param` / `@returns` 补充。
- 简单函数、显然的参数、getter、类型转换和纯展示 JSX 不强制模板化注释；不逐行翻译代码。重点解释鉴权、敏感数据、并发、幂等、取消和降级为何如此处理。

## 日志与追踪

- 复用既有请求、消息链、execution 和工具身份，保持异步任务的因果关联；不要新增一套并存的业务 Trace。细则见[日志设计](docs/design/logging-v2.md)和[观测协议](docs/protocol/internal/observability.md)。
- 日志只提取允许的安全摘要，未知/高风险参数默认不保存；不得先记录原始请求、响应或异常对象再依赖正则脱敏。密码、Key、Token、完整 Prompt/模型输出、源码及敏感工具参数不得进入正式日志或失败产物。
- 开始、终态、停止原因和耗时应可追踪；正常取消、拒绝与降级不伪装成异常。流式日志不逐片段记录，Provider usage 只使用厂商报告，缺失保持未知。
- 日志 schema、事件、错误语义或持久化策略变化时，同步相关设计、解析器和回归；错误码以[注册表](docs/protocol/error-codes.md)为准。日志是诊断信息，不替代业务数据库或协议事实。

## 按风险验证

- 选择能证明本次改动的测试层级，命令见[测试指南](docs/testing/README.md)。纯文档检查链接、命令与实现一致性；低风险样式或说明调整可用已有相关测试/截图，不机械新增测试或运行全套回归。
- 后端行为使用 pytest/httpx；认证、资源授权、凭据和迁移变更覆盖相关安全矩阵。涉及原子认领、配额、版本竞争或并发写入时，增加确定性的并发测试并隔离数据库状态。
- Agent 循环、调度、取消、重试与工具测试使用确定性 fake/mock Provider。修改 WS 恢复时验证相关断线、重复/乱序、重启及快照路径；修改 MCP/进程管理时验证受影响的清理路径。
- 新增或实质改变用户操作流程时，用真实前后端和 fake Provider 的 Playwright 验证成功与关键失败/权限路径；浏览器测试不替代后端和并发测试。选择器优先可访问名称或语义。
- 真实 Provider 验证通过专用命令运行。用户已明确要求该验证，或本次采用的验收范围包含它时，在说明联网计费后执行；历史验收条款本身不触发新的收费测试。按问题选择 contract、real 或 real-world；验证完整产品 World 流程时使用既有 real-world 流程并保留可查看结果。
- 测试仅使用受控文件与隔离数据库，结束时清理自己启动的服务。目录、保留轮数与账号以测试指南和现有入口为准，不硬编码新的个人路径。截图和 trace 按数据风险采集；真实内容与凭据不得写入产物。
- 如实报告通过、失败、跳过和环境限制；不以编译、API smoke 或历史验收代替本次需要验证的行为。
