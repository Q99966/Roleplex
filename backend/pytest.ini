[pytest]
testpaths = tests
asyncio_default_fixture_loop_scope = function
# 契约测试会真实联网并消耗厂商额度，默认从普通回归中排除；
# 需要时用 `pytest tests/contract -m contract` 显式运行。
addopts = -m "not contract"
markers =
    contract: 需要真实厂商凭据、会产生费用的契约测试
filterwarnings =
    ignore::DeprecationWarning
